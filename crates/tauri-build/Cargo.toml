[package]
name = "tauri-build"
# Mimics upstream's 2.x version so [patch.crates-io] satisfies consumer
# build-dependencies declared as `tauri-build = "2"`.
version = "2.99.0"
edition.workspace = true
description = "No-op stub for the upstream tauri-build crate so user build.rs scripts compile against the debug shim."
